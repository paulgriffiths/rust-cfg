# A grammar where E is a non-terminal with an ϵ-production, and B is a
# non-terminal which does not have an ϵ-production, but which is nullable.

S → A B C
A → 'a'
B → D | E
C → 'c'
D → 'd'
E → 'e' | ϵ
