# A grammar with cycles. A is cyclic with itself (and is unrealizable), and
# B, E and F form a cyclic group with each other. C and D are not cyclic.

S → A | B | C | D
A → A
B → E | 'b'
E → F | 'e'
F → B | 'f'
C → 'c'
D → 'd'
